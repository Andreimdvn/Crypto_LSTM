gcloud ai-platform jobs submit training $JOB_NAME \
        --scale-tier basic \
		--package-path /home/moldovanndrei/Crypto_LSTM/trainer \
        --module-name $MAIN_TRAINER_MODULE \
        --job-dir $JOB_DIR \
        --region $REGION \
        --runtime-version $RUNTIME_VERSION \
        --python-version $PYTHON_VERSION \
        -- \
        --file_csv=https://datahub.io/cryptocurrency/bitcoin/datapackage.json \
        --days_to_predict=100 \
        --epochs=200 \
        --iterations=50 \
        --output_file=percent_tunning_results.cfg \
        --percentage_prediction \
        --prefix_models=percent